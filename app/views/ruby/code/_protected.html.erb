class PopularKid
  ...

  def meet(popular_kid)
    self.say_hey # PROTECTED
    popular_kid.say_hey # PROTECTED

    # This doesn't error out because
    # you're inside the class. If you
    # try to call this outside, that's
    # another story.
  end

  protected
  def say_hey
    "hey"
  end
end
